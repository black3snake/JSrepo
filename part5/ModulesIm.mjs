//import sumNew from "./ModulesEx.mjs";
import {
    sum,
    // mult as multNumber Можно переименовать переменная если такая уже есть в файле
    mult
} from "./ModulesEx.mjs";

console.log(`Sum(3 + 4) = ${sum(3,4)}`)
console.log(`Mult(23 * 5) = ${mult(23,5)}`)
